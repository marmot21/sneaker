#include <stdio.h>
#include <fcntl.h>

#define ARGS 2

int main (int argc, char * argv[])
{
	// tempature file
	int tfile;

	if( argc < ARGS)
	{
		fprintf(stderr, "invalid number of arguments\n");
		return 1;
	}

	tfile = 
	

	return 0;
}
